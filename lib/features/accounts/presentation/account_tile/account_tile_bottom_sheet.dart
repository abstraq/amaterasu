import "package:flutter/material.dart";

class AccountTileBottomSheet extends StatelessWidget {
  const AccountTileBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
